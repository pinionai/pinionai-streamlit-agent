# Use this dotenv file as a template. Copy it to `.env` and fill in your actual credentials.
# Rename this file to `.env` and do not commit it to version control.
# NOTES:
# You can generate a new client secret in the PinionAI Studio portal if desired.
# Many variables are set within PinionAI Studio itself.
# agent_id is the unique identifier of the AI agent you created in PinionAI Studio and want to serve.
# Host URL is the location of PinionAI API server URL

client_id = '<YOUR_CLIENT_ID_HERE>'
client_secret = '<YOUR_CLIENT_SECRET_HERE>'
agent_id = '<YOUR_AGENT_ID_HERE>'
host_url = 'https://api.pinionai.com' # '<PINIONAI_API_HOST_URL_HERE>'

# Slack Integration (for chat_slack.py)
SLACK_BOT_TOKEN = 'xoxb-your-bot-token'
SLACK_APP_TOKEN = 'xapp-your-app-token'

# Microsoft Teams Integration (for chat_teams.py)
TEAMS_APP_ID = 'your-microsoft-app-id'
TEAMS_APP_PASSWORD = 'your-microsoft-app-password'
PORT = 3978

# YAML Configuration Examples:
# To use deploy/prod-*/env.yaml files, set the variables there instead and ensure your deployment process loads them correctly.
# Example for prod-agent/env.yaml:
client_id: "<YOUR_CLIENT_ID_HERE>"
client_secret: "<YOUR_CLIENT_SECRET_HERE>"
agent_id: "<YOUR_AGENT_ID_HERE>"
host_url: "https://api.pinionai.com" # Change if using a different host or environment
version: "" 
# (draft, development, test, live, archived). None loads latest in progress.

# If you want to allow it to load from AIA files instead, set the host_url and version there:
host_url: "https://api.pinionai.com" # Change if using a different host or environment
version: "" 
# (draft, development, test, live, archived). None loads latest in progress.

# Optional: Slack Integration Configuration
SLACK_BOT_TOKEN: "xoxb-your-bot-token"
SLACK_APP_TOKEN: "xapp-your-app-token"

# Optional Microsoft Teams Integration Configuration
TEAMS_APP_ID: "your-microsoft-app-id"
TEAMS_APP_PASSWORD: "your-microsoft-app-password"
PORT: 8080 # Usually 8080 for Cloud Run